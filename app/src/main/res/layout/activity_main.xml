<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/ConstraintLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#5BA9C6"
    tools:context=".MainActivity">

    <!--<include layout="@layout/toolbar_layout"/>-->

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:background="@drawable/background"/>

    <TextView
        android:id="@+id/slogan_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="We're the pet picker uppers."
        android:textSize="21sp"
        android:textStyle="bold"
        android:textColor="#fff"
        android:layout_centerInParent="true"/>

    <TextView
        android:id="@+id/info_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/slogan_textView"
        android:layout_margin="24dp"
        android:layout_centerHorizontal="true"
        android:text="INFO"
        android:textColor="#fff"
        android:textSize="16sp" />

    <TextView
        android:id="@+id/location_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/hours_textView"
        android:layout_centerHorizontal="true"
        android:drawableLeft="@drawable/location"
        android:gravity="center"
        android:drawablePadding="2dp"
        android:text="location"
        android:textColor="#fff"
        android:textSize="18sp"
        android:onClick="openMap"/>

    <TextView
        android:id="@+id/hours_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:padding="5dp"
        android:layout_below="@id/info_textView"
        android:text="HOURS"
        android:textSize="16sp"
        android:textColor="#fff" />


    <Button
        android:id="@+id/contact_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:padding="16dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:text="Contact Us"
        android:textColor="#fff"
        android:background="@drawable/contact_button"
        android:onClick="contactUsButtonTapped" />

</RelativeLayout>